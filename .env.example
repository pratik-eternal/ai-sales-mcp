# Express ERP backend
BACKEND_URL=http://localhost:4000
INTERNAL_API_KEY=dev-internal-key-change-me

# ERP user id for permission checks (from backend login / seed data)
MCP_ACTING_USER_ID=

# stdio (default) — used by Cursor / Claude Desktop
MCP_TRANSPORT=stdio
